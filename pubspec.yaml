name: maseno_hub
description: Maseno Revision Hub App

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.15.0
  firebase_storage: ^11.6.0
  file_picker: ^6.1.1

flutter:
  uses-material-design: true
